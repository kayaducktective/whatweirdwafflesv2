
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>What Weird Waffles - Edit Comment</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">What Weird Waffles</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Edit Comment</h2></div>
        <br>
        <div class="text-center">
            <% if (comment) { %>
            <form action="/editComment/<%= comment.commentId %>" method="POST">
                <label for="comment">Comment:</label><br>
                <textarea id="comment" name="comment" rows="4" required><%= comment.comment %></textarea><br><br>

                <label for="username">Author:</label><br>
                <input type="text" id="username" name="username" value="<%= comment.username %>" required><br><br>

                <label for="waffletype">Waffle Type:</label><br>
                <input type="text" id="waffletype" name="waffletype" value="<%= comment.waffletype %>" required><br><br>

                <label for="pfp">Profile Picture (URL):</label><br>
                <input type="text" id="pfp" name="pfp" value="<%= comment.pfp %>" required><br><br>

                <button type="submit" class="btn btn-primary">Update Comment</button>
            </form>
            <% } else { %>
            <p>No comment found.</p>
            <% } %>
            <a href="/">Back</a>
        </div>
    </div>
</body>
</html>
