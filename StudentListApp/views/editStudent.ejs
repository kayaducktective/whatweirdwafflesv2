<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Supermarket App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Supermarket App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addProduct">Add new product</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Product</h2></div>
        <br>
        <div class="text-center">
          <% if (student) { %> 
            <form action="/editStudent/<%= student.studentId %>" method="POST" enctype="multipart/form-data">
                <label for="name">Student Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= student.name %>" required><br><br>

                <label for="dob">DOB:</label><br>
                <input type="date" id="dob" name="dob" min="0" value = "<%= student.dob %>" required><br><br>

                <label for="contact">Contact:</label><br>
                <input type="number" id="contact" name="contact" min="0" value = "<%= student.contact %>" required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value = "<%= student.image %>" disabled><br>
                <img src = "/images/<%= student.image %>" width="20%"><br><br>

                New Image:<br> <input type="file" id="image" name="image" accept= "image/*" value=<%= student.image %>><br><br>

                <button type="submit">Update Student</button>
            </form>
          <% } else { %>
              <p>No student found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>